{
	"name": "oss-reader",
	"version": "1.0.2",
	"description": "OSS-Reader is a CLI tool for reading contents from OSS bucket",
	"scripts": {
		"build": "rimraf dist && cross-env NODE_ENV=production babel src -d dist -s"
	},
	"bin": {
		"oss-reader": "./bin/index.js"
	},
	"repository": "https://github.com/VanMess/oss-reader",
	"keyword": [
		"OSS",
		"util",
		"OSS reader"
	],
	"author": "wenjie",
	"license": "MIT",
	"dependencies": {
		"ali-oss": "^6.0.2",
		"commander": "^2.19.0",
		"lodash": "^4.17.11",
		"prettysize": "^2.0.0",
		"prompt-promise": "^1.0.3"
	},
	"devDependencies": {
		"babel-cli": "^6.26.0",
		"babel-plugin-source-map-support": "^2.0.1",
		"babel-plugin-transform-object-rest-spread": "^6.26.0",
		"babel-plugin-transform-runtime": "^6.23.0",
		"babel-preset-env": "^1.7.0"
	}
}